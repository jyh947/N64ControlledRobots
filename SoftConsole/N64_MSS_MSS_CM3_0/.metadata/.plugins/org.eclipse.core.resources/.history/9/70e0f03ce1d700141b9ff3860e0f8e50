#include <stdio.h>
#include <inttypes.h>
#include "drivers/mss_uart/mss_uart.h"

int main()
{
	while( 1 )
	{
		int input = *(0x40050000);
        printf("Output: %lu\r\n", MYTIMER_getCounterVal()); // Standard printf() now work

	}
	return 0;
}
